#!/bin/bash
# Capture the arguments passed to the script
USER_ID=$1
REQ_ID=$2
# Log the start of the processing
echo "Processing job for UserID: $USER_ID, RequestID: $REQ_ID"
# Exit with success
exit 0